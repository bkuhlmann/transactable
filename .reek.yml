exclude_paths:
  - tmp
  - vendor

detectors:
  NestedIterators:
    exclude:
      - "Transactable::Pipeable#define_steps"
  TooManyStatements:
    exclude:
      - "Transactable::Pipeable#define_pipe"
      - "Transactable::Pipeable#define_steps"
